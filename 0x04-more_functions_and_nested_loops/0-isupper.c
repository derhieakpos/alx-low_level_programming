#include "main.h"
/**
 * main - Entry point
 * Description: checks for uppercase characters
 * Return: Always 0
 */
int _isupper(int c);
{
	int n;

	for (c = 0; c <= n; c++)
	{
		if (c == 'A')
		{
			return (1);
		}
		else if (c == 'a')
		{
			return (0);
		}
	}
	return (0);
}
